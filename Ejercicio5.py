#Ejercicio 5
#Podemos utilizar el siguiente código genera una lista. 

x, lista = 0, []
while x < 20:
    lista.append(x**2 % 2)
    x+=1

#Escribir la/s sentencia/s necesarias para generar la misma lista L utilizando el método de generación de listas por comprensión. 
# *Nota: no cambiar el nombre de la variable L.*

L = []
